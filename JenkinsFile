pipeline {
    agent any
     stages {
        stage('Install dependencies') {
            steps {
                bat 'npm install'
            }
        }
        stage('Run tests') {
            steps {
                echo 'npm test'
            }
        }
        stage('Run app') {
            steps {
                bat 'npm run start &'
            }
        }
}
